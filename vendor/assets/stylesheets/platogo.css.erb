.swagger-ui-wrap {
  max-width: 80%;
  min-width: 960px;
  margin-left: auto;
  margin-right: auto;
}

#header {
  background-color: #333;
}

#header a#logo {
  background-image: url(<%= image_url 'platogo-logo.png' %>);
  background-size: 30px 30px;
}

.resource > div .options {
  display: none !important;
}
